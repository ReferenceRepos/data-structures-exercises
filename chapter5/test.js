/*'use strict';

var expect = require('chai').expect;
var p = require('./palindrome');

describe('Output', function() {
  it('gets an accurate output', function() {
    expect(p.palindrome("pop")).to.eql(true);
  });
});*/
